# Beads (local runtime state)
.beads/
.dolt/
*.db

# Working state (disk-persisted agent outputs)
.workflows/

# Archive (pre-plugin personal commands)
.archive/

# Claude memory (private workflow preferences)
.claude/memory/

# Local command copies (workaround for plugin char budget limit)
.claude/commands/

# Editor
*.code-workspace

