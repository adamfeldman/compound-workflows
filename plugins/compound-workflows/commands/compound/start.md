---
name: compound:start
description: (deprecated) Use /do:start instead
---

> **Deprecated:** This command moved to `/do:start` in v3.0.0.

The user's original arguments: #$ARGUMENTS

Invoke the `/do:start` skill immediately with the arguments shown above. Do not ask for new input.

If the user explicitly asked for `/compound:start`, mention that it has been renamed to `/do:start`.
