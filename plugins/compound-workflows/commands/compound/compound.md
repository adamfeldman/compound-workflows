---
name: compound:compound
description: (deprecated) Use /do:compound instead
---

> **Deprecated:** This command moved to `/do:compound` in v3.0.0.

The user's original arguments: #$ARGUMENTS

Invoke the `/do:compound` skill immediately with the arguments shown above. Do not ask for new input.

If the user explicitly asked for `/compound:compound`, mention that it has been renamed to `/do:compound`.
