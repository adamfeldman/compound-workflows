---
name: compound:compact-prep
description: (deprecated) Use /do:compact-prep instead
---

> **Deprecated:** This command moved to `/do:compact-prep` in v3.0.0.

The user's original arguments: #$ARGUMENTS

Invoke the `/do:compact-prep` skill immediately with the arguments shown above. Do not ask for new input.

If the user explicitly asked for `/compound:compact-prep`, mention that it has been renamed to `/do:compact-prep`.
