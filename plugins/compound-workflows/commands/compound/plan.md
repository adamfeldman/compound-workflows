---
name: compound:plan
description: (deprecated) Use /do:plan instead
---

> **Deprecated:** This command moved to `/do:plan` in v3.0.0.

The user's original arguments: #$ARGUMENTS

Invoke the `/do:plan` skill immediately with the arguments shown above. Do not ask for new input.

If the user explicitly asked for `/compound:plan`, mention that it has been renamed to `/do:plan`.
