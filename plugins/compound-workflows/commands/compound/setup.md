---
name: compound:setup
description: (deprecated) Use /do:setup instead
---

> **Deprecated:** This command moved to `/do:setup` in v3.0.0.

The user's original arguments: #$ARGUMENTS

Invoke the `/do:setup` skill immediately with the arguments shown above. Do not ask for new input.

If the user explicitly asked for `/compound:setup`, mention that it has been renamed to `/do:setup`.
