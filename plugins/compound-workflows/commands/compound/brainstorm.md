---
name: compound:brainstorm
description: (deprecated) Use /do:brainstorm instead
---

> **Deprecated:** This command moved to `/do:brainstorm` in v3.0.0.

The user's original arguments: #$ARGUMENTS

Invoke the `/do:brainstorm` skill immediately with the arguments shown above. Do not ask for new input.

If the user explicitly asked for `/compound:brainstorm`, mention that it has been renamed to `/do:brainstorm`.
