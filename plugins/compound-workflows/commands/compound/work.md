---
name: compound:work
description: (deprecated) Use /do:work instead
---

> **Deprecated:** This command moved to `/do:work` in v3.0.0.

The user's original arguments: #$ARGUMENTS

Invoke the `/do:work` skill immediately with the arguments shown above. Do not ask for new input.

If the user explicitly asked for `/compound:work`, mention that it has been renamed to `/do:work`.
